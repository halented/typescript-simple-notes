import Note from "./Note"

interface Props { }

function NotesContainer(props: Props) {
    const { } = props

    return (
        <div>
            grab container from antd
            <Note />
        </div>
    )
}

export default NotesContainer
