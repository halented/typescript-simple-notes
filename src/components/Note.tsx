import { Divider } from 'antd'
import React from 'react'

interface Props { }

function Note(props: Props) {
    const { } = props

    return (
        <div>card from antd</div>
    )
}

export default Note
